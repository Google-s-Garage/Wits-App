<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thewitsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".photographyUploader"></activity>
        <activity android:name=".photographyActivity" />
        <activity android:name=".creativesActivity" />
        <activity android:name=".overflowComment" />
        <activity android:name=".inputQuestion" />
        <activity android:name=".overflow" />
        <activity
            android:name=".MainActivity"
            android:parentActivityName=".Login" />
        <activity
            android:name=".Register"
            android:parentActivityName=".Login" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".SafeSpace" />
        <activity android:name=".safeSpaceDiscussions" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

</manifest>